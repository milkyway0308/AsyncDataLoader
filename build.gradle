plugins {
    id "org.sonarqube" version "3.2.0"
}

sonarqube {
    properties {
        property "sonar.projectKey", "milkyway0308_AsyncDataLoader"
        property "sonar.organization", "milkyway0308"
        property "sonar.host.url", "https://sonarcloud.io"
    }
}